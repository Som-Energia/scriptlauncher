test:
  description: scripts de test
  scripts:
    test_parameter:
      title: Passing a parameter
      description: 
      script: /usr/bin/printf {nouser}
      parameters:
        nouser:
          default: "It works\n"

    test_fileupload:
      title: Search in file
      description: Uploads a file and search a regular expression on it
      script: /bin/grep -R "{expr}" "{file}"
      parameters:
        expr:
          userdescription: Expressió regular
        file:
          type: FILE

    test_multifileupload:
      title: 'Example: Uploading fails'
      description: This script concatenate files
      script: /bin/cat {param1} {param2} {param3}
      parameters:
        param1:
          type: FILE
        param2:
          type: FILE
        param3:
          type: FILE

    test_filedownload:
      title: 'Example: Downloading file'
      description: This script download file
      script: 
        - /bin/tar 
        - cvfz
        - "{output1}"
        - scriptlauncher.py
      parameters:
        output1:
          type: FILEDOWN

    pythonExample:
      title: Reverse
      description: How to execute python code
      script:
      - python
      - -c
      - phrase='{phrase}'; print ' '.join(reversed(phrase.split()))
      parameters:
        phrase:
          userdescription: Phrase to reverse

